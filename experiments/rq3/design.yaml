# What it answers the learning rate:
# How much should I update my beliefs after receiving new information (stimulus)

# More detail:
# - It represents the speed of adaptation
# - How fast it reacts to new experiences


base_config: experiments/base.yaml

grid:
  learning:
    learning_rate:
      - 0.90
      # - 0.60
      # - 0.30
  randomness:
    seed:
      - 42
      - 5
      # - 23
  demand:
    n_agents:
      # - 1300
      # - 1550
      - 2000
  due_convergence: 
    duaIterate_max_iterations: 
      - 1